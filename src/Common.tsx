import styled from "styled-components";

export const BigButton = styled.button`
  min-height: 48px;
  min-width: 96px;
  border-radius: 16px;
  margin: 24px;
  font-size: 32px;

  &:focus {
    outline: none;
  }
`;
